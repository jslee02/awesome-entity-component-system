name: Refresh Metadata

on:
  schedule:
    - cron: "0 8 * * 3"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  refresh:
    uses: jslee02/awesome-list-infra/.github/workflows/refresh-metadata.yml@main
